# LuLuCamera Android 分步骤开发计划

更新日期：2026-09-20。本文同时记录实现进度；勾选表示代码或文档已经写入，不自动代表 CI、真机、GPU 或用户验收通过。

当前已有 Android 客户端、高清服务、CI、测试和交付文档。正式角色资源、逐人像素掩码、真机性能与覆盖结果、真实生成模型质量和内测数据仍然缺失，因此 M0、M1、M2 尚未验收完成。

## 1. 产品目标

开发一款 Android 噜噜相机：用户打开相机，点击画面中的某个人，为其选择噜噜 A、噜噜 B 或真人。选中的人物实时变成水豚角色，跟随人物的位置、大小和基础动作；拍照后可以立即保存，也可以生成更自然、更精细的高清噜噜照片。

```text
打开相机 → 点击人物 → 选择 A / B / 真人 → 实时跟随动作
                                             ↓
                                          拍照预览
                                             ↓
                              立即保存 / 高清生成 / 重拍
                                             ↓
                                      高清结果预览与保存
```

实时链路使用端侧视觉识别和 3D 角色驱动；生成式 AI 只处理拍照后的静态图片。第一阶段验证：变身是否有趣、用户能否自然理解点选人物、结果是否值得保存或分享。

### 1.1 交付里程碑

| 里程碑 | 必须交付的结果 | 目的 |
| --- | --- | --- |
| M0：技术原型 | Android 真机上简单噜噜跟随单人移动和挥手，能导出合成图 | 验证实时变身路线 |
| M1：离线内测版 | A/B 角色、点选、基础多人、前后摄像头、拍照预览、相册保存 | 提前测试核心体验 |
| M2：完整 MVP | M1 + 拍后高清、失败恢复、性能验收、可安装 APK | 完成产品闭环 |

高清生成仍是完整 MVP 的必需功能。M1 可以提前内测，但不能视为完整 MVP 已完成。

### 1.2 功能范围

| 优先级 | 功能与边界 |
| --- | --- |
| P0 | Android 相机预览、前后摄像头、拍照、预览、保存相册 |
| P0 | 点击人物选择 A/B/真人；每个人独立保存选择 |
| P0 | 单人位置、尺度、身体倾斜、挥手、基础腿部动作、有限侧身与头部朝向 |
| P0 | 最多 3 人的基础多人替换，支持不同人选择不同角色；主要支持正常光线、无严重遮挡场景 |
| P0 | 拍后高清生成、任务状态、失败恢复、结果保存；保护未选人物与编辑区外背景 |
| P0 | 权限拒绝、人物丢失、相机中断、性能下降和网络失败的明确反馈 |
| P1 | 视频、系统分享入口、表情同步、离场后身份恢复、复杂交叉追踪、手持物遮挡优化 |
| 暂不做 | 实时扩散模型、自动判断性别、复杂服装生成、高度角色定制、登录体系、社区 |

原计划中“多人分别选择不同噜噜”的优先级前后矛盾，这里统一纳入基础多人 P0；复杂遮挡留在后续版本。结果页分享按钮随 P1 实施，MVP 通过保存相册完成输出。

### 1.3 默认假设与待固化条件

- 目标为 Android 手机，第一版按竖屏交互设计；仍正确处理传感器旋转、照片方向和前置镜像。
- 暂以 Android 10 / API 29 为最低系统候选，使用一台中端、一台高端真机验证。最终支持范围在步骤 1 根据模型与渲染要求固化。
- 先完成单人，再加入基础多人；完整 MVP 不用单人版本替代多人目标。
- 噜噜为可映射人体动作的拟人水豚，A/B 共用骨架。具体外观以用户提供或确认的素材为准，占位模型不等于正式 IP。
- 8 周是 Android、视觉/图形、角色美术和 AI 工作可以并行时的参考节奏，不是保证工期。一人开发时按里程碑推进，在 M0 后重新估算。
- 测试手机、正式角色素材、远程仓库、高清服务运行环境与预算、正式分发渠道，应在对应步骤开始前明确；缺失时推进不依赖它们的工作。

## 2. 必须提前验证的技术问题

### 2.1 角色覆盖不等于恢复背景

人物遮住的背景在当前帧不可见。删除或模糊人物像素不能恢复背景，放大噜噜也不保证遮住伸出的手脚和头发。

第一版先采用“角色覆盖 + 姿态约束”：通过角色比例和动作映射覆盖正常站立、挥手和走动中的真人。设定缩放上限，避免为了覆盖一人而挡住旁人。严重遮挡、置信度低或覆盖失败时，平滑恢复该人物的真人画面，并提示调整姿势。

M0 必须检查真人残留、侧身、双人靠近和角色误盖旁人的实拍结果。若正常动作也经常穿帮，先修改角色比例、动作映射或合成路线；不能把模糊人物当作无痕替换，也不能把问题留到最后一周。

### 2.2 逐人掩码与稳定身份是两件事

“所有人的前景掩码”无法支持只替换某个人。视觉模块必须输出可分别关联到姿态的独立掩码，再由 Tracker 分配稳定 ID；模型结果顺序和单帧实例编号不能直接作为长期人物 ID。

MediaPipe Pose Landmarker 支持配置检测人数与分割掩码输出，作为首选验证路线。掩码与姿态的对应关系、多人分离质量和真机速度仍需实测；不满足要求时才比较独立实例分割方案。[MediaPipe Android 指南](https://ai.google.dev/edge/mediapipe/solutions/vision/pose_landmarker/android)

### 2.3 同一帧、同一坐标系

预览裁剪、分析图、掩码、角色、触摸和保存图片使用统一坐标映射；每份观测携带帧号与时间戳，前置镜像按明确规则只执行一次。CameraX 的输出需要处理裁剪和旋转映射。[CameraX 坐标变换](https://developer.android.com/media/camera/camerax/transform-output)

第一版拍照保存同帧相机底图与角色合成结果。若增加独立高分辨率静态拍摄，必须重新计算该照片的姿态、掩码与人物对应关系，不能直接套用上一张预览帧的数据。

### 2.4 高清背景保护由合成保证

不能只靠提示词要求 AI 不改背景。后端将生成区域与原图合成，编辑区外直接保留原图像素。编辑区包含原人物轮廓、目标噜噜轮廓及边缘过渡区；仅使用原人物掩码可能裁掉较宽的噜噜。

多人生成保留逐人角色绑定和掩码，保护未选人物。编辑区重叠时明确处理顺序；无法可靠区分前后关系时返回可理解的失败原因，不生成串人或覆盖旁人的结果。

## 3. Android 技术方案

以下方案遵循“成熟库直接接入、官方示例按需迁移、项目只实现业务差异”。具体版本、模型和渲染集成方式在 M0 实测后锁定；开源调研与选择依据见第 3.1 节。

| 层 | 起步选择 | 实施约束 |
| --- | --- | --- |
| 语言与 UI | Kotlin、Jetpack Compose | UI 承载按钮和状态，不把每帧大图放进 Compose 状态流 |
| 相机 | CameraX + Google Camera Samples | 迁移官方相机示例，复用生命周期与输出变换能力，不自建 Camera2 封装 |
| 姿态与掩码 | MediaPipe Tasks Pose Landmarker | 先 1 人再 3 人，实测 CPU/GPU；运行模型随包提供，支持离线 |
| 人物追踪 | ByteTrack 基线 + 项目薄适配层 | 优先评估已有实现的 Android 接入；自定义逐人角色绑定与低置信度回退 |
| 3D 渲染 | 优先验证 SceneView 的纯 3D 模块，底层 Filament/gltfio | 复用加载、场景和资源生命周期；仅在接口受限时直接调用 Filament |
| 画面合成 | 优先验证 CameraX CameraEffect 接入 | OverlayEffect 用于轮廓/调试等二维覆盖；3D 纹理与掩码组合需专项验证 |
| 角色资产 | Blender 源文件 → GLB | A/B 共用骨架、坐标轴、尺度和资源契约 |
| 相册 | MediaStore | 按系统要求写入，不申请无关的全量相册读取权限 |
| 网络 | HTTPS、OkHttp、Kotlin 协程 | 复用上传、取消、连接管理；不另写 HTTP 客户端或重复网络层 |
| 客户端任务 | Room + 按需使用 WorkManager | 持久化任务和文件引用；需要跨进程继续执行的工作交给系统调度 |
| 高清推理 | Hugging Face Diffusers | 复用 Inpainting、ControlNet、IP-Adapter、LoRA 加载；只编排经过兼容验证的一条管线 |
| 高清服务 | FastAPI + Huey/SQLite 候选 + 生成适配器 | 复用队列和执行器，项目实现任务契约、幂等、授权与成本控制 |
| 构建与分发 | Gradle Wrapper、GitHub Actions | 自动检查和产出 APK，由用户手动下载安装验收 |

Filament 提供 Android 渲染库和 glTF 加载组件，但相机、掩码、骨骼与导出的完整链路仍需项目集成验证。[Filament 官方仓库](https://github.com/google/filament)

```text
CameraX → FramePacket ───────────────────────────┐
               ↓                                │
      Pose + 逐人 Mask → Tracker → 逐人角色选择    │
                              ↓                 │
                    平滑 / 动作映射 / 骨骼驱动    │
                              ↓                 ↓
                           角色渲染 ────────→ 同帧合成
                                                ↓
                                           预览 / 快照
```

分析采用只保留最新待处理帧的背压策略，明确缓冲区所有权和关闭时机；推理与渲染解耦，避免旧帧排队。使用 CameraX 对应分析机制后仍需测量全链路延迟。[CameraX 图像分析](https://developer.android.com/media/camera/camerax/analyze)

### 3.1 开源项目调研与复用决策

调研日期：2026-09-20。通过公开网页和 GitHub 检索 Android 相机、全身 Avatar、姿态/分割、追踪、渲染和局部图像编辑，核对下列仓库 README、许可证信息、官方 API 及部分发布记录。本次未下载模型、编译或真机测试；“推荐复用”表示有公开能力依据，不表示已证明适合本项目的性能要求。

结论：有相似项目和大量成熟组件，但本次检索未找到可以直接改名使用、完整覆盖“Android 原生 + 多人点选 + 水豚替换 + 拍后高清”的开源成品。应组合成熟组件，不从零写通用基础设施；也不把虚拟主播 Demo 当作已解决真人覆盖的相机。

| 项目 / 官方入口 | 已有能力与当前证据 | 本项目决策和边界 |
| --- | --- | --- |
| [Android Camera Samples](https://github.com/android/camera-samples) | Google 官方示例；当前目录包含拍照、分析、特效等示例，README 说明 Apache 许可头 | **优先迁移示例**：相机生命周期、切镜头、拍照、存储；只取需要的模块，不整套搬入示例目录、主题和架构依赖；迁移文件前核对其许可头 |
| [MediaPipe Android Pose 示例](https://github.com/google-ai-edge/mediapipe-samples/tree/main/examples/pose_landmarker/android) | 官方 Tasks 示例；代码 Apache-2.0，模型许可单独核对 | **直接接入 Tasks，迁移示例适配代码**；姿态模型、推理调度不重写；独立掩码和稳定 ID 仍由实测与 Tracker 验证 |
| [SceneView](https://github.com/sceneview/sceneview) | Apache-2.0；Compose/Filament、GLB、骨骼动画；[发布记录](https://github.com/sceneview/sceneview/releases)可见 2026-09-16 的 v4.37.0 | **优先验证纯 3D 模块**：模型、场景、生命周期；不因它支持 AR 就引入 ARCore。播放预制动画不等于可直接驱动噜噜骨骼，合成/导出仍需试验 |
| [Filament](https://github.com/google/filament) | Apache-2.0；Android 渲染和 glTF 加载组件、官方示例 | **作为底层能力复用**；SceneView 接口不满足骨骼/纹理需求时下探；不自写渲染引擎、GLB 解析器和蒙皮系统 |
| [CameraX OverlayEffect](https://developer.android.com/reference/androidx/camera/effects/OverlayEffect) | 官方提供 Canvas 覆盖、帧时间戳和变换元数据 | **用于二维轮廓与同步验证**；不是 3D 人物替换器。核对选定版本的目标组合，不因继承常量出现 `IMAGE_CAPTURE` 就假定任意拍照组合都支持 |
| [ByteTrack](https://github.com/FoundationVision/ByteTrack) / [ByteTrack-cpp](https://github.com/Vertical-Beach/ByteTrack-cpp) | MIT；前者为算法官方实现，后者为不绑定检测器的 C++17 实现，依赖 Eigen/CMake，仓库包含对照测试；后者未提供可直接采用的 Android 发布包 | **先复用追踪基线**，评估 C++/JNI 和维护成本；不能宣称是即插即用 Kotlin 库，也不能期待它自带掩码/姿态关联和角色绑定 |
| [Diffusers](https://github.com/huggingface/diffusers) | Apache-2.0 代码；有持续[发布记录](https://github.com/huggingface/diffusers/releases)，提供现成编辑管线 | **正式推理首选**；不自写采样器、模型加载、LoRA 训练框架。每组基础模型、适配器和权重单独检查兼容与许可 |
| [ComfyUI](https://github.com/Comfy-Org/ComfyUI) | GPL-3.0；图形工作流和 API，适合试验编辑组合 | **可选实验工具**；固定工作流、节点与权重版本。若直接作为生成服务，就通过统一适配器使用，不同时重复实现相同 Diffusers 管线；第三方节点另行核对 |
| [Huey](https://github.com/coleifer/huey) | MIT；轻量任务队列，支持 SQLite、重试和 Worker；[发布记录](https://github.com/coleifer/huey/releases)可见 3.4.0 | **小规模后端队列候选**；优先 SQLite 避免新增 Redis 运维。业务幂等、任务所有权、GPU 超时与进程中断恢复仍需实现和验证 |

客户端通用能力同时复用 [Room](https://developer.android.com/training/data-storage/room)、[WorkManager](https://developer.android.com/develop/background-work/background-tasks/persistent) 和 [OkHttp](https://github.com/square/okhttp)。Room 存任务元数据与文件引用，不存整张照片；WorkManager 处理需要可靠恢复的上传/下载/清理，前台状态刷新用协程。WorkManager 不是秒级定时器，也不保证用户强制停止后立即执行。

相似产品和不宜直接接入的候选：

| 项目 | 相似之处 | 不直接采用的原因 |
| --- | --- | --- |
| [KalidoKit / Kalidoface](https://github.com/yeemachine/kalidokit) | MediaPipe 关键点驱动全身角色，包含姿态到旋转求解；MIT | 仓库明确标记 deprecated，主要是 JavaScript/VRM 路线。可按许可迁移必要数学映射和测试思路，不引入旧 Web 推理栈；不据弃用公告推断当前 Android Tasks 已自带完整 Rig 求解 |
| [XR Animator](https://github.com/ButzYung/SystemAnimatorOnline) | 摄像头全身动作捕捉、Avatar 驱动，包含 Android 浏览器 AR 体验 | 作者将源码改编许可列为 CC BY-NC-SA 4.0，不属于可无条件用于产品的宽松开源依赖；这里只作体验参照，不迁移源码。模型资产也有独立许可 |
| [TrackerForAll](https://github.com/realmoctopus/TrackerForAll) | Unity 人体追踪、分割、角色驱动，功能描述接近 | 仓库明确只有文档，不提供完整源码；不能当作开源实现复用 |

### 3.2 接入顺序与必须自定义的范围

1. **先复用官方相机和 Pose 示例**，跑通相机 → 姿态/掩码的数据链；不先造新的相机框架或视觉运行时。
2. **再验证 SceneView + Filament + CameraX 的最小贯通**：同一摄像头、一个绑定 GLB、一份掩码、一次合成导出。避免重复相机所有权，避免每帧 CPU 读回再上传 GPU；无法通过时记录具体接口与耗时，再选择直接 Filament 路线。
3. **追踪先对照 ByteTrack**：输入现有检测框，不附带再运行一套 YOLO。优先复用兼容实现；只有 Android 接入/依赖成本不合适且有证据时，才有限移植必要关联逻辑，保留来源和回放对照。姿态作为辅助条件由本项目加在适配层。
4. **高清先调用现成管线**：从 Diffusers 的 [ControlNet Inpaint](https://huggingface.co/docs/diffusers/api/pipelines/controlnet) 与 [IP-Adapter 示例](https://github.com/huggingface/diffusers/blob/main/docs/source/en/using-diffusers/ip_adapter.md)选取兼容组合，不把任意模型/适配器拼接当成可用方案。ComfyUI 只在缩短验证时间时使用。
5. **最后接成熟任务组件**：Room/WorkManager 与 Huey 负责通用存储和调度；LuLuCamera 定义任务业务状态、访问边界和用户反馈。

本项目仍需实现的核心差异：逐人点选与角色绑定、人体到水豚的比例和动作映射、跨帧同步、覆盖残留判断、遮挡与失跟回退、逐人高清编辑区和背景保护、相机与生成结果的产品交互。这些是组件集成与产品逻辑，不能以“已使用开源库”替代真机验收。

采用前记录仓库 URL、tag/commit、制品版本、许可/NOTICE、模型及资产来源、取用文件与修改范围；常规库优先制品依赖，示例仅迁移必要代码，不把整个上游仓库复制进来。首次实际引入时建立第三方依赖记录。版本号是调研快照，不能直接当作已经验证的兼容组合。

## 4. 工程结构和契约

目录按对应步骤创建，不预先堆空目录；创建前明确职责、命名和清理规则。根目录 `AGENTS.md` 继续作为项目规范。

| 路径 | 职责与约定 |
| --- | --- |
| `android/` | Android 工程、Gradle Wrapper 和锁定版本配置；首版单 app 模块，内部按职责分包 |
| `android/app/src/main/.../camera/` | 相机、帧数据、坐标转换、快照 |
| `android/app/src/main/.../vision/` | 模型适配、姿态与独立掩码 |
| `android/app/src/main/.../tracking/` | 匹配、轨迹生命周期、角色绑定 |
| `android/app/src/main/.../character/` | 骨架契约、角色加载、动作映射和平滑 |
| `android/app/src/main/.../rendering/` | 渲染、合成、输出纹理与读回 |
| `android/app/src/main/.../generation/` | 上传、任务状态、取消、恢复与下载 |
| `android/app/src/main/.../ui/` | Camera、Preview、Result 与错误/权限状态 |
| `android/app/src/main/assets/` | 随包模型、运行时 GLB、角色清单；记录来源、版本和校验值 |
| `backend/` | API、生成适配器、任务执行、临时存储；后端依赖单独锁定 |
| `assets/characters/` | 美术源文件、参考图、骨架说明和授权记录；大资源按体积选择版本化存储 |
| `docs/` | 中文架构决策、API 契约、验收用例与结果；报告按日期和构建号命名 |
| `tests/fixtures/` | 获授权的小型固定测试素材和标签，不提交用户照片 |
| `.github/workflows/` | 构建、检查、artifact 工作流，不自动部署 |
| `experiments/` | 按主题命名的技术验证；结论迁入 docs 后清理可再生成中间产物 |

`build/`、下载缓存、用户照片、生成结果和运行日志不进源代码版本管理。开发临时文件放系统临时目录，任务结束清理；运行照片缓存按会话或明确期限清理。

| 数据对象 | 必要字段 |
| --- | --- |
| `FramePacket` | 帧号、时间戳、尺寸、旋转、镜像、裁剪/变换、图像引用 |
| `PersonObservation` | 所属帧、单帧人物编号、框、关键点及置信度、独立掩码 |
| `PersonTrack` | Track ID、轨迹状态、最近观测、平滑姿态、选择的角色、最后出现时间 |
| `CharacterDefinition` | 角色 ID、资源版本、GLB、骨骼映射、比例/关节限制、参考图 |
| `CaptureSnapshot` | 同帧原图、实时结果、逐人选择/姿态/掩码、坐标和方向、资源版本 |
| `GenerationJob` | Job ID、幂等键、状态、错误码、结果位置、创建和过期时间 |

轨迹状态采用 `tentative → tracked → lost → removed`。短暂丢失可保留选择，但不能将旧选择转给新人物；长时间离场后重进按新人处理，跨离场身份识别留在 P1。

## 5. 分步骤实施

状态统一使用“未开始 / 进行中 / 阻塞 / 已完成”。有交付物和验收证据才可标记完成，不能按日历自动跳过失败关卡。

截至 2026-09-20，步骤 1～11 均为“进行中”，步骤 12 已准备材料但尚未开始真实内测。主要代码已经写入，但 CI 尚未远程执行，本机缺少完整 Android SDK、GPU 和可用真机，正式 A/B GLB 与授权评测素材也未提供。因此三个里程碑均未通过验收。逐项证据与缺口见 `docs/verification-status.md`。

### 步骤 1：建立 Android 工程与 APK 交付通路

依赖：无。

- [x] 补充 Android 目录约定和中文 README，记录包名候选、设备基线、最低系统和构建方式。
- [x] 创建 Kotlin/Compose 最小工程与 Gradle Wrapper，锁定兼容的 JDK、AGP、Kotlin、SDK 和依赖版本，不使用动态 `+` 版本。
- [x] 按第 3.1 节固定拟复用的官方示例提交与库版本，记录第三方许可；先验证最小依赖组合，不整仓复制示例或预装所有候选库。
- [x] 配置 GitHub Actions：依赖还原、编译、lint、单元测试、APK artifact。
- [x] 区分带应用 ID 后缀的内测 APK 与正式签名产物；正式签名只读取环境 secrets，密钥不入库。
- [ ] 确定中端与高端真机及系统版本，检查模型/GPU 最低要求，固化实际支持范围。

交付：工程骨架、CI、中文说明、空壳 APK。

验收：CI 产出 APK，用户在基线设备手动安装启动。缺少远程仓库或 CI 权限时先完成配置并标记未执行，不为触发 CI 自动 push。

### 步骤 2：打通相机、帧数据和坐标映射

依赖：步骤 1。

- [x] 从 Android Camera Samples 迁移所需预览、切镜头和生命周期模式，补充本项目权限拒绝与中断恢复交互；正式拍照属于步骤 7。
- [x] 在 CameraX 已有输出变换能力上建立薄 `FramePacket` 适配，集中处理方向、裁剪、镜像和触摸坐标，不重写已有坐标工具。
- [x] 分离分析线程与 UI，处理背压及缓冲区释放，加入帧号和推理耗时信息。
- [x] 为 FILL_CENTER 裁剪、镜像和逆变换补充 JVM 坐标用例；真机标记对齐仍待验收。

交付：相机 APK、坐标变换用例、真机记录。

验收：两台设备连续预览 10 分钟不崩溃，切换镜头和返回前台可恢复，标记不因镜像/裁剪发生错位。

### 步骤 3：验证姿态、逐人掩码和性能预算

依赖：步骤 2。

- [x] 复用官方 Pose Android 示例的模型初始化、Delegate、异步回调和帧输入处理，接入单人姿态与掩码；只保留一套由本项目管理的 CameraX 会话。
- [ ] 试验 2～3 人，分别着色显示姿态和掩码，核对一一关联能力。
- [ ] 比较输入分辨率、模型规格、CPU/GPU、推理频率；记录耗时分位数、内存和温升。
- [x] 定义低置信度及观测过期规则，不把缺失关键点当作正常坐标。
- [ ] 记录半身、侧身、暗光、遮挡和双人靠近的失败样例，决定是否需要替代分割方案。

交付：视觉原型、固定素材清单、模型和参数选型记录。

验收：正常光线单人稳定输出；分开的 3 人可分别关联掩码与姿态。多人分离或预算不通过时先调整路线，再推进完整多人功能。

### 步骤 4：验证角色、渲染和合成，完成 M0

依赖：步骤 2；掩码覆盖验收依赖步骤 3。角色准备可从步骤 1 同期开始。

- [ ] 先复用 SceneView 纯 3D 示例的加载与生命周期，在 Filament 上验证逐帧骨骼更新、透明边缘和图像导出；不重写 GLB 解析、蒙皮和场景管理。
- [ ] 优先试验 CameraX CameraEffect 的合成接入，OverlayEffect 先用于二维轮廓；单独验证角色纹理、掩码、帧时间戳和导出路径，不能假定 Canvas 覆盖等于 3D 合成。
- [ ] 明确唯一合成输出路径和支持的 UseCase 组合。SceneView 不满足时记录原因后下探 Filament；最终快门导出实际合成图，不能只截 Compose 界面。
- [x] 定义 A/B 共用的根节点、骨盆、脊柱、头和四肢骨骼命名、静止姿态、坐标轴与尺度。
- [x] 在矢量占位角色上实现位置/尺度跟随与简单挥手；全身参考头脚跨度，半身回退到可靠躯干尺度，避免脚踝缺失导致跳变。正式 GLB 仍待接入。
- [ ] 对比真人掩码与角色覆盖，实拍检查头发、手脚残留和误盖旁人。
- [ ] 记录支持姿势、回退条件和路线结论；失败时修正比例、映射或合成方法。

交付：M0 APK、演示录像、GLB 契约、合成和性能结论。

验收：真机单人移动、靠近、远离和挥手时角色跟随，可导出与预览一致的图。正常动作覆盖未通过，M0 不得标为完成。

### 步骤 5：完成人物追踪和点选

依赖：步骤 3、4。

- [ ] 先用 ByteTrack 官方实现回放已有检测框，记录 ID 切换和丢失基线；评估 ByteTrack-cpp 的 Android/JNI 集成，不为 Tracker 再引入检测模型。
- [x] 完成 Android 接入评估并记录 ADR：ByteTrack-cpp 需要 NDK/Eigen 且没有 Android 制品，M0 先采用最多 3 人的姿态辅助薄关联层；保留可替换边界，若真机交叉不达标再做 ByteTrack 对照。
- [ ] 优先按独立掩码命中点击，框作辅助；当前 MediaPipe 只记录掩码可用性，像素级命中尚未接通。
- [x] 实现每个 Track 独立的 A/B/真人选择，选择器按需出现。
- [x] 匹配不确定时暂停替换、恢复真人，避免角色串人。
- [x] 处理镜头切换、离场、短暂遮挡和最多 3 人的状态；双人交叉及上限提示仍需真机完善。

交付：Tracker、点选 UI、固定轨迹回放用例。

验收：移动不丢失选择；两人分别选择 A/B 互不影响；简单交叉不串人，严重遮挡可安全回退。

### 步骤 6：完成 A/B 角色和动作映射

依赖：步骤 4；多人绑定验收依赖步骤 5。

- [ ] 接入 A/B 正式或已确认测试资产、缩略图、参考图，验证共用驱动逻辑。
- [x] 实现躯干倾斜、抬手/挥手、腿部动作和有限头部参数；不承诺单目输入的精确全角度姿态。
- [ ] 参考 KalidoKit 的姿态求解与既有测试思路，只在需要时按 MIT 许可迁移必要数学逻辑；噜噜映射层加入关节限幅、角度压缩、左右约束与静止姿态偏置，不引入弃用的 Web 运行栈。
- [x] 对位置、尺度和动作使用 EMA；是否采用 One Euro Filter 等待真机抖动与延迟数据。
- [x] 关键点不可靠时停止角色绘制，Track 短暂丢失保留选择，过期后移除，不让角色长期漂浮。

交付：角色资源、映射配置、动作对照录像。

验收：站立、挥手、抬臂、走动、轻微侧身均能辨认动作；A/B 可区分，无明显骨骼翻转和尺度突变。

### 步骤 7：完成拍照、预览与保存，交付 M1

依赖：步骤 5、6。

- [x] 实现 Camera、Preview 页面；预览提供原图/噜噜对比、保存、高清入口和重拍。
- [x] 快门锁定同一分析帧的图像、角色、姿态和编辑掩码，后台编码保存，不阻塞 UI；当前编辑掩码是人物框扩边，逐人像素掩码仍待步骤 3 实测接入。
- [x] 在预览明确显示实际分析帧尺寸，不把实时快照冒充独立传感器高清照片。
- [x] 通过 MediaStore 写入相册，失败时删除未完成媒体项，并用忙状态阻止重复点击；方向和前置镜像仍待真机验收。
- [x] AI 未配置时明确提示暂不可用；普通拍照、预览和保存不发起网络请求。

交付：M1 离线 APK、拍照保存验收记录。相册实现参考 [Android 共享媒体存储指南](https://developer.android.com/training/data-storage/shared/media)。

验收：两台设备可完成点选、替换、拍照、对比、保存和重拍；保存图与快门画面一致，不含按钮和调试轮廓。

### 步骤 8：提前验证高清生成路线

依赖：角色参考图与获授权样例，不必等待 M1；最终输入对齐步骤 7。

- [ ] 建立 A/B、不同姿势、单人/多人、背景和未选人物的固定评测集。
- [ ] 优先调用 Diffusers 已有 Inpainting/ControlNet/IP-Adapter 管线，验证一组基础模型与适配器；不自写采样、权重加载或通用扩散推理。若用 ComfyUI 快速试验，导出固定工作流并明确最终唯一执行路径。
- [x] 在假生成器与正式适配器出口实现可控编辑区和原图回贴，自动测试编辑区外像素不变；真实模型的边缘、肢体、未选人物和角色身份仍未验收。
- [ ] 参考图不足以稳定角色时再复用 Diffusers 现成 LoRA 训练/加载示例；100～300 张仅作为素材筹备量级，不是训练必要条件或效果保证。
- [ ] 记录单张耗时、显存、成本、失败率，锁定输出尺寸和模型/资源版本。

交付：对比记录、可复现输入输出、推荐方案、单次成本和时延预算。

验收：A/B 可辨认、人物绑定正确、编辑区外保持原图。未通过时继续调整路线，不能把训练完成等同于高清功能完成。

### 步骤 9：实现最小高清服务

依赖：步骤 8；接口契约可提前完成。

- [x] 固化上传契约：原图、可选实时合成参考、姿态/角色、编辑区、图像方向、资源版本和幂等键，见 `docs/generation-api.md`；逐人像素掩码暂由粗编辑区替代。
- [x] 实现 `POST /generations` 创建、`GET /generations/{jobId}` 查询、`DELETE /generations/{jobId}` 请求取消/清理，并增加受权结果下载端点。
- [x] 状态包含 `queued / running / completed / failed / cancelled / expired`；返回稳定错误码、可否重试和结果过期时间。
- [x] 图像使用 multipart 文件上传，SQLite 持久化任务；服务启动将中断的 `running` 任务标为可重试失败。
- [x] API 与耗时任务分离，接入 Huey SQLite 队列；运行说明固定单 Worker，业务入口限制活跃任务数。
- [ ] 对队列执行中的进程退出、GPU 卡死和重复投递做恢复验证；队列持久化不等于业务 exactly-once，幂等仍由任务记录保证。取消排队任务与中止正在推理的任务分别定义。
- [x] 加入匿名会话任务隔离、请求大小、内存限流、活跃任务上限、结果过期与清理；生产反向代理和 GPU 超时仍需部署环境验证。
- [x] 供应商密钥只在后端；提供配置模板和假生成器，正式地址由构建配置提供，普通用户不手填。

交付：API 契约、可测试服务、假生成适配器、运行与清理说明。

验收：重复请求不重复计费，不能读取其他会话任务；失败、超时、取消和重启有确定结果。无 GPU 的 CI 用假适配器验证逻辑，真实生成质量单独验收。

### 步骤 10：接通高清生成与失败恢复

依赖：步骤 7、9。

- [x] 首次点击高清时说明上传内容和保留期；普通预览和拍照保持端侧。
- [x] 用 OkHttp 承担上传、查询、下载和取消；页面展示离散任务状态，不虚构百分比。
- [x] 用 Room 持久化 Job ID、幂等键和文件引用，WorkManager 唯一任务负责上传、轮询、下载和重试，不建立后台常驻服务。
- [x] 原图和即时噜噜图先写应用私有目录，高清失败仍可保存、重拍或重试。
- [x] 下载限制大小并解码检查尺寸，处理过期状态；服务端默认保留 24 小时，客户端私有文件默认保留 7 天并定期清理。
- [x] 客户端与服务端代码不记录照片内容或访问凭证，断网时 WorkManager 等待且本地照片不丢失；仍待真机进程死亡测试。

交付：完整高清流程 APK、网络异常与恢复记录。

验收：单人和多人保留正确角色选择；断网、切后台、杀进程、服务失败不产生重复任务或丢失本地照片。

### 步骤 11：性能、多机型和完整 MVP 验收

依赖：步骤 10。性能测量从步骤 2 持续进行，本步集中解决剩余问题。

- [ ] 按第 7 节测试单人、3 人、前后摄像头及连续运行。
- [x] 加入分析频率节流：常态约 15 FPS，最近推理超过 90 ms 时降至约 8 FPS；渲染复杂度降级仍需真机数据决定。
- [x] 当前以 CPU 为基线；模型初始化失败时相机预览仍保留并显示错误，GPU 回退仍待实际 GPU 路径接入验证。
- [ ] 复查生命周期、内存增长、发热、照片方向、相册写入和真实服务可达性。
- [x] CI 已配置 Android lint、单测、APK 和报告 artifact，以及后端 pytest 报告；远程工作流尚未执行，暂无实际下载链接。
- [x] 更新实际依赖版本、许可和复用边界；正式模型/角色资产出处和原生库真机兼容性仍待资源到位后核对。

交付：M2 候选 APK、测试报告、已知限制、可复现版本和资源清单。

验收：P0 与第 7 节门槛通过，不能用“已知问题”替代核心功能验收。构建成功仅代表构建通过，真机人工验收才是发布依据。

### 步骤 12：小范围内测与下一版决策

依赖：步骤 11 和用户人工验收。

- [x] 准备 20～50 人内测说明、隐私草案和反馈模板，约定先用 5 人检查流程。
- [ ] 经用户安排分发 APK 或使用其指定渠道，不自动发布、部署或发消息邀请。
- [ ] 记录是否能无指导点选变身、首次保存耗时、保存意愿、高清尝试及成功情况。
- [ ] 使用自愿反馈和必要的本地统计，不为内测加入账号或收集原始相机画面。
- [ ] 按结果排列 P1，优先修复阻碍拍照的失败，再决定分享、视频、遮挡、表情和更多角色。

交付：反馈汇总和下一版优先级。用户觉得有趣、能自主操作并愿意保存，是继续投入的主要依据。

## 6. 依赖与参考排期

主链：`1 → 2 → 3 → 4 → 5 → 6 → 7 → 10 → 11 → 12`。角色资产从第 1 周开始；`8 → 9` 提前独立推进，在步骤 10 与客户端合并。

| 周次 | Android / 视觉 / 图形 | 角色与 AI | 出口 |
| --- | --- | --- | --- |
| 第 1 周 | 步骤 1～3、渲染小样 | 角色契约、占位资源、生成样例准备 | APK 和初步实测 |
| 第 2 周 | 步骤 4、开始步骤 5 | 角色迭代、高清路线小样 | M0，确认路线可继续 |
| 第 3 周 | 完成步骤 5、推进步骤 6 | 骨架动作调整、步骤 8 初评 | 追踪点选可用 |
| 第 4 周 | 完成步骤 6、开始步骤 7 | 角色定版、步骤 9 | 动作与拍照联调 |
| 第 5 周 | 完成步骤 7 | 完成步骤 9、质量复查 | M1 离线版 |
| 第 6 周 | 步骤 10 | 多人高清、异常恢复 | 完整功能候选版 |
| 第 7 周 | 步骤 11 | 成本、清理与质量回归 | M2 候选和验收报告 |
| 第 8 周 | 修复问题、步骤 12 | 反馈分析 | 人工通过后小范围内测 |

每步指定一位实际负责人。Android 工程师负责相机和交互，视觉/图形能力覆盖追踪与合成，3D 美术负责模型和绑定，AI 能力覆盖生成与服务。人员可兼任，工期随实际人力调整。这里的并行工作线是项目排期建议，不代表自动启用多代理执行开发。

复用后优先把前两周投入官方示例、SceneView 与合成链路贯通，把步骤 8～10 改为现成推理/任务组件的接入验证。暂不据此缩短 8 周排期：角色素材、真人覆盖、多人关联和真机稳定性仍在关键路径，待 M0 给出实际工时后再调整。

## 7. 可测量的验收标准

以下是计划目标，不是现有成绩。第 1 步记录机型、系统、输出分辨率、模型规格和性能档位，所有报告注明配置与样本数。正常光线、人物完整或躯干清晰、最多 3 人且无严重遮挡为主要支持场景。

| 项目 | 初始门槛 | 测量方式 |
| --- | --- | --- |
| 相机启动 | 已授权后首个有效预览帧 p95 ≤ 2 秒 | 每设备 20 次，系统授权时间单列 |
| 人物识别 | 入镜到稳定可选 p95 ≤ 1 秒 | 单人 30 次，3 人单列 |
| 点击反馈 | 轮廓/选择器反馈 p95 < 150 ms | 每设备 30 次，不等待下一次推理才反馈 |
| 实时画面 | 目标 30 FPS；连续 10 分钟每个 10 秒窗口平均 ≥ 25 FPS | 单人和 3 人分别记录，注明降级配置 |
| 动作跟随 | 人体动作到角色变化 p95 ≤ 150 ms，争取 < 100 ms | 高帧率录像或全链路时间戳，不只量模型耗时 |
| 位置与抖动 | 躯干锚点误差 p95 ≤ 人物框高度 5%；静止锚点波动 p95 ≤ 2% | 固定素材至少 5 段，统一尺度比较 |
| 基础追踪 | 支持场景 20 段各 30 秒录像中串人 0 次 | 含移动和双人简单交叉；严重遮挡另测回退 |
| 覆盖效果 | 固定支持姿态帧至少 95% 无可见真人头/肢体残留，无明显误盖未选人物 | 至少 100 帧人工标注；回退帧另计，不算覆盖成功 |
| 快门反馈 | 到可见冻结画面 p95 < 500 ms | 编码与相册落盘另测 |
| 拍照保存 | 权限和存储正常时，200 次端到端尝试成功率 ≥ 99% | 两台设备均有样本，记录成功数、总数和失败原因 |
| 输出一致性 | 快照与保存图的角色、方向、裁剪一致 | 前后摄像头、A/B、多人；无按钮/调试元素 |
| AI 区域保护 | 无损合成结果编辑区外像素差为 0 | 统一色彩空间比较，有损编码影响单列 |
| AI 角色质量 | 40 个固定样例中 ≥ 90% 通过身份、绑定、肢体、边缘联合检查 | A/B、单人/多人均覆盖，分组报告 |
| AI 时延与成本 | 步骤 8 实测后设定 p50/p95、单张预算及超时上限 | 记录尺寸、环境、排队时间；M2 前必须固化 |
| 稳定性 | 每设备连续 30 分钟无崩溃、ANR 或持续内存增长 | 含 10 分钟实时、切后台、拍照和高清操作 |
| 离线与恢复 | 断网仍可拍照保存，恢复不重复创建任务 | 权限、网络、杀进程和服务失败用例全部验证 |

CI 重点测试坐标变换、Tracker 状态、同帧快照、任务幂等和掩码外保护。相机画质、动作自然度、GPU 性能与发热必须真机验证，模拟器或假服务不能替代。

## 8. 执行规则与下一步

- 本地只使用已有工具编辑和轻量检查，不为验证安装或升级 SDK、IDE、全局工具；正式还原、build、test 和打包由 GitHub Actions 完成。
- 未执行的 CI、真机或 GPU 验证标记“未验证”，静态检查不写成运行通过。
- 不自动 `git push`、部署或发布。CI 产出供人工下载的 artifact；正式签名和分发在产物可审查后按用户指令处理。
- 每步结束更新状态、交付路径、证据、限制与下一步；路线变更先改文档再实现。
- 开发每个通用模块前先核对第 3.1 节的复用入口；默认依赖成熟库或迁移官方示例。需要替代或自写时，先记录现成方案无法满足的具体证据，不以个人技术偏好重复造轮子。
- 外部条件缺失时继续可独立完成的配置、代码和假服务验证，但不声称对应里程碑完成。

下一步先在远程仓库手动触发 CI，修复真实编译或测试问题并下载 Debug APK；随后用两台基线真机完成 M0/M1 验收。正式 A/B GLB、逐人掩码与授权评测集到位后，再进行 GPU 质量/成本实验和 M2 内测，不以当前占位实现跳过这些关卡。
